{
  "installCommand": "bash build-vercel.sh && npm install",
  "functions": {
    "api/generate.js": {
      "maxDuration": 30
    }
  },
  "routes": [
    { "src": "/api/generate", "dest": "/api/generate.js" }
  ]
}